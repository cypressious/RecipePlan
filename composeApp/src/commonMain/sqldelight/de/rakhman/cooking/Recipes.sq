CREATE TABLE recipe
(
    id    TEXT PRIMARY KEY NOT NULL,
    title TEXT             NOT NULL,
    url   TEXT
);

selectAll
:
SELECT *
FROM recipe;

deleteAll
:
DELETE
FROM recipe;

insert
:
INSERT INTO recipe(id, title, url)
VALUES (?, ?, ?);